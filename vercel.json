{
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "cd artifacts/dean-dashboard && node scripts/gen-public-data.mjs && node scripts/gen-affinity.mjs && pnpm exec vite build --config vite.config.ts",
  "outputDirectory": "artifacts/dean-dashboard/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/data/(.*)", "destination": "/api/data?f=$1" },
    { "source": "/((?!api/|data/).*)", "destination": "/index.html" }
  ]
}
